[package]
name = "similarity-core-ts"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similarity-core = { git = "https://github.com/mizchi/similarity", package = "similarity-core" }
